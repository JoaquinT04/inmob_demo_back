{
  "name": "@inmob/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Tipos, constantes y schemas compartidos entre frontend y backend",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "zod": "^3.24.0"
  }
}
